# Summary

## Examples

- **ex_hello_world** – Minimal SFC + reactive state (`examples/ex_hello_world`)
- **ex_fetch_composable** – Simple `useFetch` composable with error/loading (`examples/ex_fetch_composable`)

## Projects

- **thin-dashboard** (`projects/thin-dashboard`) – Minimal dashboard shell with routing and a mock API client.

## Architecture Docs

- ADR‑0001: Vue 3 + Vite + TS as defaults
- ADR‑0002: Keep state local; avoid global stores unless necessary
